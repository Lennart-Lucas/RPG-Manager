name: random_table_engine
description: JSON-driven random table generation engine.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.4

dependencies:
  uuid: ^4.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
